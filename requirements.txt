# Trixie ships PEP 668; install.sh installs python3-dbus + python3-gi via apt
# and only this file via pip --break-system-packages. Kept as a single-line
# manifest so `pip install -r requirements.txt --break-system-packages` works
# directly when bypassing the installer.
bluezero
